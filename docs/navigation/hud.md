# HUD 与灵动岛

HUD 用于在游戏画面上方显示路线或自由探索图层、玩家位置和资源缓存图；灵动岛用于紧凑显示当前用途、提示和状态。自由探索会复用路线导航同款地图框，但隐藏路线节点、路线警告和路段接续提示。

## 交互

- `Ctrl + Alt + H`：切换灵动岛锁定或点击穿透。
- 按住 `Alt`：临时解锁并拖动灵动岛。
- “软件设置”页可完整配置灵动岛；“开始导航”页右侧也提供整体缩放、地图缩放、边缘黑框透明度和位置重置快捷项。两处使用同一组全局设置并保持同步，导航运行期间仍可调整。

## 显示坐标

HUD 玩家位置使用原始追踪坐标。路线吸附位置仅用于路线展示，避免资源点和真实位置随路线发生偏移。

路线导航与自由探索统一使用：底图 → 累计涂抹 → 路线或候选路线 → 当前实际轨迹 → 节点与资源点 → 玩家光标。自由探索会隐藏路线相关层。区域涂抹与轨迹记录共用经过防抖的未吸附真实坐标；八种带名称的色弱友好预设和自定义 `#RRGGBB` 颜色会同步作用于所有地图预览与历史涂抹。

## 常见现象

- **指针不动但坐标变化**：恢复默认刷新间隔并重启追踪，参见[算法与参数](/tracking/algorithms)。
- **灵动岛超出边框**：先重置位置；仍存在时提交截图、主题、缩放比例和窗口尺寸。
- **无法拖动**：确认灵动岛未处于锁定或点击穿透状态，按住 `Alt` 再拖动。

